{
  "name": "glyphcut",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Subset fonts and convert to WOFF2 in your browser. Licensed font files never leave your device.",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "woff2-encoder": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "jsdom": "^25.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.0.0"
  }
}
